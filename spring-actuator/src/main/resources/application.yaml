management:
  endpoint:
    shutdown:
      enabled: true
    health:
      show-details: always
  endpoints:
    web:
      exposure:
        include: "*"
  info:
    java:
      enabled: true
    os:
      enabled: true
    env:
      enabled: true
  server:
    port: 9292

info:
  app:
    name: "hello-actuator"
    company: "chanho"


logging:
  level:
    hello.controller: debug